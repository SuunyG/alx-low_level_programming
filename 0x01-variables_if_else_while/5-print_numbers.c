#include <stdio.h>

/**
 * main - this code prints all the alphabet lower case
 * Return: the code returns 0.
 */
int main(void)
{
	int numb;

	for (numb = 0; numb < 10; numb++)
	{
		printf("%d", numb);
	}
	printf("\n");
	return (0);
}
